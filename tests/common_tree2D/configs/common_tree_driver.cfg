simulation_parameters
{
    nLevels=3;
    refinement_factor=0.125;
    nStart=101;
    nTotal=3;
    nskip=1;

    output
    {
        field_dir=output_test1;
        directory=output_test2;
    }

    tree_file_prefix = tree_info_common_tree_;
    flow_file_prefix = flow_common_tree_;
    resume=true;
    restart_file=./common_tree_restart_2d.txt;


    domain
    {
        bd_base = (-64,-64);
        bd_extent = (128,128);

        # Lx=32.0;
        dx_base=0.25;
        block_extent=8;

        # block
        # {
        #     base=(-20,-64);
        #     extent=(128,128);
        # }
    }
}
