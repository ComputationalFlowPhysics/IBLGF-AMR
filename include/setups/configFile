simulation_parameters
{
    L    = 3;
    domain{
        max_extent   = 50000;
        block_extent = 4;

        block
        {
            base   = (0,0,0);
            extent = (32,32,32);
        }
    }

}
