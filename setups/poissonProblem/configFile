simulation_parameters
{
    nLevels=2;
    L=1;

    domain{

        Lx=5;
        max_extent   = 500;
        block_extent =8;
       
        block
        {
            base   = (0,0,0);
            extent = (64,64,64);
        }
        block
        {
            base   = (64,64,0);
            extent = (64,64,64);
        }
    }
}
