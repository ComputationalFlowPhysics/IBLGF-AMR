simulation_parameters
{
    // Used by test_POD.cpp/test_POD_Test.cpp
    nLevels=0;
    nStart=100;
    nTotal=6;
    nskip=1;

    // Reference data consumed by POD2D constructor.
    tree_ref_file=./output/tree_info_100.bin;
    flow_ref_file=./output/flowTime_100.hdf5;

    output
    {
        directory=output;
        field_dir=output;
    }

    domain
    {
        bd_base = (-128,-128);
        bd_extent = (256,256);

        Lx=20.0;
        dx_base=0.25;
        block_extent=8;

        block
        {
            base=(-128,-128);
            extent=(256,256);
        }
    }
}
